export default function Dashboard() {
  return (
    <section>
      <h1 className="mb-2 text-2xl font-semibold">Dashboard</h1>
      <p className="text-sm text-muted-foreground">
        server 状态与今日用量概览;阶段 5.2 接入 /admin/status + /admin/stats。
      </p>
    </section>
  );
}
